# Supabase Configuration
# For local development (using supabase-selfhost Docker Compose)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_ANON_KEY=

# Database Configuration
# For local development (using supabase-selfhost Docker Compose)
# Default password is "postgres" - change in supabase-selfhost/.env if needed
DATABASE_URL=
DIRECT_URL=

# Optional: Service Role Key (only for Edge Functions/cron, never in Route Handlers)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: API URL (defaults to relative if not set)
# NEXT_PUBLIC_API_URL=http://localhost:3000

# ============================================
# For Production (Cloud Supabase)
# ============================================
# Replace the above values with your production Supabase credentials:
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-production-anon-key
# SUPABASE_ANON_KEY=your-production-anon-key
# DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres?sslmode=require"
# DIRECT_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres?sslmode=require"
